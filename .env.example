DISCORD_TOKEN=replace_with_your_bot_token
# Railway PostgreSQL provides this automatically when the database service is linked.
DATABASE_URL=postgresql://user:password@host:5432/database
# Optional: sync slash commands to one server immediately while developing.
TEST_GUILD_ID=
